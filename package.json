{
  "name": "lamiss-youtube-transcript-poc",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/build.js",
    "test": "npm run build && node test/youtubeUrl.test.js",
    "phase3:verify": "npm run build && node scripts/phase3-verify.js"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "playwright": "^1.63.0",
    "typescript": "5.4.5"
  }
}
